@inject IUserSessionService SessionService
@rendermode InteractiveServer

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">
            <i class="bi bi-ticket-perforated"></i>
            سیستم تیکتینگ سازمانی
        </a>
        @if (currentUser != null)
        {
            <div class="d-flex align-items-center text-white">
                <span class="me-3">
                    خوش آمدید، @currentUser.FirstName @currentUser.LastName
                    @if (currentUser.Role == UserRole.Admin)
                    {
                        <span class="badge bg-danger ms-1">مدیر</span>
                    }
                    else if (currentUser.Role == UserRole.Manager)
                    {
                        <span class="badge bg-info ms-1">سرپرست</span>
                    }
                    else if (currentUser.Role == UserRole.HelpDesk)
                    {
                        <span class="badge bg-warning ms-1">پشتیبانی</span>
                    }
                    else if (currentUser.Role == UserRole.Client)
                    {
                        <span class="badge bg-secondary ms-1">مشتری</span>
                    }
                </span>
                <a href="/logout" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-box-arrow-left"></i> خروج
                </a>
            </div>
        }
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        @if (currentUser != null)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <span class="bi bi-house-door-fill" aria-hidden="true"></span> داشبورد
                </NavLink>
            </div>

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="tickets">
                    <span class="bi bi-ticket-perforated" aria-hidden="true"></span> تیکت‌های من
                </NavLink>
            </div>

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="tickets/create">
                    <span class="bi bi-plus-circle" aria-hidden="true"></span> ایجاد تیکت
                </NavLink>
            </div>

            @if (currentUser.Role >= UserRole.HelpDesk)
            {
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="tickets/assigned">
                        <span class="bi bi-person-check" aria-hidden="true"></span> تیکت‌های تخصیص یافته
                    </NavLink>
                </div>

                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="tickets/all">
                        <span class="bi bi-list-ul" aria-hidden="true"></span> تمام تیکت‌ها
                    </NavLink>
                </div>
            }

            @if (currentUser.Role == UserRole.Manager)
            {
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="tickets/client-tickets">
                        <span class="bi bi-people-fill" aria-hidden="true"></span> تیکت‌های مشتریان
                    </NavLink>
                </div>
            }

            @if (currentUser.Role == UserRole.Admin)
            {
                <hr class="mx-3 text-secondary" />
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="admin/users">
                        <span class="bi bi-people" aria-hidden="true"></span> کاربران
                    </NavLink>
                </div>

                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="admin/reports">
                        <span class="bi bi-graph-up" aria-hidden="true"></span> گزارشات
                    </NavLink>
                </div>

                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="admin/notifications">
                        <span class="bi bi-bell" aria-hidden="true"></span> ارسال اعلان
                    </NavLink>
                </div>
            }
        }
    </nav>
</div>

@code {
    private User? currentUser;

    protected override async Task OnInitializedAsync()
    {
        currentUser = await SessionService.GetCurrentUserAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Refresh user data on first render
            currentUser = await SessionService.GetCurrentUserAsync();
            StateHasChanged();
        }
    }
}

